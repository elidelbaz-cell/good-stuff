{
  "name": "neon-sprawl",
  "version": "1.0.0",
  "private": true,
  "description": "2D pixel side-scroller: tentacle-swing through an alien city, work the quest line, steal the last rocket.",
  "type": "module",
  "scripts": { "build": "node build.mjs" },
  "devDependencies": { "esbuild": "^0.20.0" }
}
